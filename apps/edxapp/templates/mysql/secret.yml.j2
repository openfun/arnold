apiVersion: v1
kind: Secret
metadata:
  labels:
    app: edxapp
    service: mysql
  name: "{{ edxapp_mysql_secret_name }}"
  namespace: "{{ project_name }}"
data:
  MYSQL_ROOT_PASSWORD: "{{ MYSQL_ROOT_PASSWORD | default('') | b64encode }}"
  MYSQL_ALLOW_EMPTY_PASSWORD: "{{ MYSQL_ALLOW_EMPTY_PASSWORD | default('yes') | b64encode }}"
  MYSQL_DATABASE: "{{ MYSQL_DATABASE | default('edxapp') | b64encode }}"
  MYSQL_USER: "{{ MYSQL_USER | default('edxapp_user') | b64encode }}"
  MYSQL_PASSWORD: "{{ MYSQL_PASSWORD | default('password') | b64encode }}"
