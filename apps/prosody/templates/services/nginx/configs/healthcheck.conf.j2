server {
  listen {{ prosody_nginx_healthcheck_port }};
  server_name localhost;

  location {{ prosody_nginx_healthcheck_endpoint }} {
    add_header Content-Type text/plain;
    return 200 "OK";
  }

  location {{ prosody_nginx_status_endpoint }} {
     stub_status;
  }

}
