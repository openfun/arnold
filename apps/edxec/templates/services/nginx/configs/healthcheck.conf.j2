server {
  listen {{ edxec_nginx_healthcheck_port }};
  server_name localhost;

  location {{ edxec_nginx_healthcheck_endpoint }} {
    add_header Content-Type text/plain;
    return 200 "OK";
  }

  location {{ edxec_nginx_status_endpoint }} {
     stub_status;
  }
}
