apiVersion: v1
kind: Endpoints
metadata:
  labels:
    app: mysql
  # name of the endpoint should be the same as the corresponding service
  name: mysql
  namespace: "{{ project_name }}"
subsets:
  - addresses:
    - ip: {{ mysql_ip }}
    ports:
    - port: 3306
      name: 3306-tcp
