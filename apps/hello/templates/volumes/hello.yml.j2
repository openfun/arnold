apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hello-pvc
  namespace: "{{ project_name }}"
  labels:
    app: hello
    deployment_stamp: "{{ deployment_stamp }}"
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Mi
