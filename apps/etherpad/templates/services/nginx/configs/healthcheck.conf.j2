server {
  listen {{ etherpad_nginx_healthcheck_port }};
  server_name localhost;

  location {{ etherpad_nginx_healthcheck_endpoint }} {
    add_header Content-Type text/plain;
    access_log off;
    return 200 "OK";
  }

  location {{ etherpad_nginx_status_endpoint }} {
     stub_status;
  }
}
