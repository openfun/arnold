server {
  listen {{ nextcloud_nginx_healthcheck_port }};
  server_name localhost;

  location {{ nextcloud_nginx_healthcheck_endpoint }} {
    add_header Content-Type text/plain;
    return 200 "OK";
  }
}
