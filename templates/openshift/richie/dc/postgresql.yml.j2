apiVersion: v1
kind: DeploymentConfig
metadata:
  labels:
    app: postgresql
  name: postgresql-richie
  namespace: "{{ project_name }}"
spec:
  replicas: 1
  selector:
    app: postgresql
    deploymentconfig: postgresql
  template:
    metadata:
      labels:
        app: postgresql
        deploymentconfig: postgresql
    spec:
      containers:
        - image: centos/postgresql-96-centos7
          name: postgresql
          ports:
          - containerPort: "5432"
            protocol: TCP
          env:
            - name: POSTGRESQL_DATABASE
              value: "{{ richie_database.name }}"
          envFrom:
            - secretRef:
                name: richie
