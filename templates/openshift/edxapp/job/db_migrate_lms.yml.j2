apiVersion: batch/v1
kind: Job
metadata:
  name: edxapp-dbmigrate-lms
  namespace: "{{ project_name }}"
spec:
  template:
    metadata:
      name: edxapp-dbmigrate-lms
    spec:
      containers:
      - name: edxapp-dbmigrate-lms
        image: "{{ edxapp_image }}:{{ edxapp_tag }}"
        command: ["python", "manage.py", "lms", "migrate", "--settings", "docker_run"]
        volumeMounts:
        - mountPath: /config
          name: edxapp-config
      volumes:
      - name: edxapp-config
        configMap:
          defaultMode: 420
          name: edxapp
      restartPolicy: Never
