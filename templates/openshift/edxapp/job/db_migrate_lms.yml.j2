apiVersion: batch/v1
kind: Job
metadata:
  name: edxapp-dbmigrate-lms-{{ deployment_stamp }}
  namespace: "{{ project_name }}"
  labels:
    app: edxapp-lms
    version: "{{ edxapp_tag }}"
    deployment_stamp: "{{ deployment_stamp }}"
spec:
  template:
    metadata:
      name: edxapp-dbmigrate-lms-{{ deployment_stamp }}
      labels:
        app: edxapp-lms
        version: "{{ edxapp_tag }}"
        deployment_stamp: "{{ deployment_stamp }}"
    spec:
      containers:
      - name: edxapp-dbmigrate-lms
        image: "{{ edxapp_image }}:{{ edxapp_tag }}"
        command: ["python", "manage.py", "lms", "migrate", "--settings", "docker_run"]
        volumeMounts:
        - mountPath: /config
          name: edxapp-config
      volumes:
      - name: edxapp-config
        configMap:
          defaultMode: 420
          name: edxapp
      restartPolicy: Never
