apiVersion: v1
kind: Service
metadata:
  labels:
    app: mysql
  # name of the service should be database host name in settings
  name: mysql
  namespace: "{{ project_name }}"
spec:
  ports:
  - name: 3306-tcp
    port: 3306
    protocol: TCP
    targetPort: 3306

