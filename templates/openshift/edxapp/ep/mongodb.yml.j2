apiVersion: v1
kind: Endpoints
metadata:
  labels:
    app: mongodb
  # name of the endpoint should be the same as the corresponding service
  name: mongodb
  namespace: "{{ project_name }}"
subsets:
  - addresses:
    - ip: 192.168.10.11
    ports:
    - port: 27017
      name: 27017-tcp
