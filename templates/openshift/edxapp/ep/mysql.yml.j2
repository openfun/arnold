apiVersion: v1
kind: Endpoints
metadata:
  labels:
    app: mysql
  # name of the endpoint should be the same as the corresponding service
  name: mysql
  namespace: "{{ project_name }}"
subsets:
  - addresses:
    - ip: 192.168.10.254
    ports:
    - port: 3306
      name: 3306-tcp
