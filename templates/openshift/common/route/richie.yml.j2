apiVersion: v1
kind: Route
metadata:
  labels:
    app: richie
  name: richie
  namespace: "{{ project_name }}"
spec:
  host: {{ richie_host }}
  port:
    targetPort: {{ richie_port }}-tcp
  tls:
    termination: edge
  to:
    kind: Service
    name: nginx
    weight: 100
